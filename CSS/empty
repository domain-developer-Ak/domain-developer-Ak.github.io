/* styles.css */

body {
    margin: 0;
    font-family: Arial, sans-serif;
}

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #141414; /* Dark background */
    padding: 15px;
    border-radius: 60px;
}

.logo {
    color: #28F100; /* Green text for the logo */
    font-size: 20px;
    font-weight: bold;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

.nav-links {
    list-style: none;
    display: flex;
    margin: 0;
    padding: 0;
}

.nav-links li {
    margin-left: 20px;
}

.nav-links a {
    color: #28F100; /* Green text for links */
    text-decoration: none;
    font-size: 16px;
    font-weight: 100;
    padding: 10px;
}

.nav-links a:hover {
    color: white; /* White text on hover */
}

/* Add styles for icons after the nav-links */
.icons {
    display: flex;
    align-items: center;
}

.icon {
    margin-left: 20px;
    /* Add specific styles for each icon based on your preference */
}

/* Responsive design */
@media screen and (max-width: 768px) {
    .navbar {
        flex-direction: column;
        align-items: center;
    }

    .nav-links, .icons {
        margin-top: 10px;
    }
}
